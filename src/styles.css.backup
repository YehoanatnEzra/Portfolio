/* =========================================================
   Design tokens
   - Edit these to tweak the global look & feel
   ========================================================= */
:root {
  --bg: #0b0c10;
  --panel: #12141a;
  --text: #e8ebf1;
  --muted: #a7b0c0;
  --accent: #06b6d4;
  --accent-2: #8b5cf6;
  --ring: #06b6d455;
  --shadow: 0 10px 25px rgba(0, 0, 0, .25);
  --radius: 18px;
  --container: 1100px;
}

/* Light theme overrides */
html[data-theme="light"]{
  --bg: #f6f7fb;
  --panel: #ffffff;
  --text: #0b1324;
  --muted: #4b5563;
  --shadow: 0 10px 25px rgba(2, 6, 23, .08);
}

/* =========================================================
   Base / reset
   ========================================================= */
*,
*::before,
*::after { box-sizing: border-box; }

html, body { min-height: 100%; }

body{
  margin: 0;
  font-family: system-ui, -apple-system, Segoe UI, Roboto, Helvetica, Arial, "Noto Sans", "Segoe UI Emoji";
  background: var(--bg);
  color: var(--text);
  line-height: 1.6;
}

/* Hide scrollbars globally (keeps scrolling via wheel/keys/touch) */
html, body, * {
  -ms-overflow-style: none; /* IE and Edge */
  scrollbar-width: none;    /* Firefox */
}
html::-webkit-scrollbar,
body::-webkit-scrollbar,
*::-webkit-scrollbar {
  width: 0;
  height: 0;
  background: transparent;
}

a { color: inherit; text-decoration: none; }

img{
  max-width: 100%;
  display: block;
  transition: transform .3s ease, filter .3s ease;
}

.container{
  width: min(100% - 32px, var(--container));
  margin-inline: auto;
}

/* Focus ring for keyboard users */
a:focus-visible,
button:focus-visible,
input:focus-visible {
  outline: 2px solid var(--accent);
  outline-offset: 2px;
  border-radius: 6px;
}

/* =========================================================
   Header / Navigation
   ========================================================= */
header{
  position: sticky;
  top: 0;
  z-index: 50;
  backdrop-filter: blur(10px);
  -webkit-backdrop-filter: blur(10px);
  background: rgba(0, 0, 0, .4);
  border-bottom: 1px solid rgba(255, 255, 255, .2);
  color: #fff;
}

.nav{
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 12px 0;
}

.nav a{
  opacity: .95;
  text-shadow: 0 1px 6px rgba(0,0,0,.45);
}

.nav-links{ /* used on Projects header */
  display: flex;
  gap: 18px;
  align-items: center;
}

.brand{ font-weight: 800; letter-spacing: .3px; }

/* =========================================================
   Hero (Home)
   ========================================================= */
.hero{
  position: relative;
  min-height: 100vh;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: flex-start;
  color: #fff;
  text-align: left;
  overflow: hidden;
}

.hero .inner{
  position: relative;
  z-index: 1;
  display: flex;
  align-items: flex-start;
  gap: 40px;
  max-width: 1100px;
  padding: 80px 40px 40px;
}

.headline-title{
  display: flex;
  align-items: center;
  gap: 12px;
}

.headline-title img{
  width: 80px;
  height: 80px;
  border-radius: 50%;
  object-fit: cover;
  box-shadow: 0 4px 12px rgba(0,0,0,.35);
}

.headline h1{
  font-size: clamp(38px, 6vw, 72px);
  margin: 0 0 6px;
  text-shadow: 0 3px 18px rgba(0,0,0,.55);
}

.headline p{
  margin: 0 0 20px;
  font-size: 22px;
  color: #eef2f7;
  text-shadow: 0 2px 12px rgba(0,0,0,.6);
}

.about h2{
  font-size: 28px;
  margin: 0 0 12px;
  color: #fff;
  text-shadow: 0 2px 12px rgba(0,0,0,.7);
}

.about p{
  font-size: 18px;
  line-height: 1.75;
  margin: 0 0 1.8em;
  max-width: 760px;
  color: #fff;
  text-shadow: 0 2px 12px rgba(0,0,0,.7);
}

.quick{
  display: grid;
  grid-template-columns: repeat(12, 1fr);
  gap: 14px;
}

.qcard{
  grid-column: span 4;
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 14px;
  padding: 16px;
  border-radius: 16px;
  border: 1px solid rgba(255,255,255,.3);
  background: rgba(0,0,0,.25);
  backdrop-filter: blur(10px);
  box-shadow: 0 4px 15px rgba(0,0,0,.2);
  color: #fff;
}

.qcard h3{ margin: 0; font-size: 18px; }

.section-title{
  font-size: 22px;
  margin: 0 0 12px;
  color: #fff;
  text-shadow: 0 2px 12px rgba(0,0,0,.7);
}

.skill-track{
  display: flex;
  gap: 16px;
  overflow-x: auto;
  overscroll-behavior-x: contain;
  scroll-snap-type: x mandatory;
  padding-bottom: 10px;
  -webkit-overflow-scrolling: touch;
}

.skill-card{
  scroll-snap-align: start;
  min-width: 360px;
  max-width: 420px;
  border-radius: 16px;
  border: 1px solid rgba(255,255,255,.3);
  background: rgba(0,0,0,.25);
  backdrop-filter: blur(10px);
  box-shadow: 0 4px 15px rgba(0,0,0,.2);
  color: #fff;
  padding: 18px 18px 14px;
}
/* Hide scrollbar but keep scrolling */
.scroll-wrap {
  overflow-x: auto;
  -ms-overflow-style: none;  /* IE + Edge */
  scrollbar-width: none;     /* Firefox */
}
.scroll-wrap::-webkit-scrollbar { display: none; }

/* =========================================================
   Projects page: toolbar, chips, grid, cards, modal
   ========================================================= */
main { padding: 28px 0 48px; }

.toolbar{
  display: flex;
  flex-wrap: wrap;
  gap: 12px;
  align-items: center;
  justify-content: space-between;
  margin-bottom: 18px;
}

.search{
  flex: 1 1 280px;
  display: flex;
  align-items: center;
  gap: 8px;
  background: var(--panel);
  border: 1px solid color-mix(in oklab, var(--text), transparent 90%);
  padding: 10px 12px;
  border-radius: 12px;
}

.search input{
  flex: 1;
  border: none;
  outline: none;
  background: transparent;
  color: var(--text);
  font-size: 14px;
}

.filters{
  display: flex;
  gap: 8px;
  flex-wrap: wrap;
}

/* Chips (language filters) */
.chip{
  padding: 8px 12px;
  border-radius: 999px;
  border: 1px solid color-mix(in oklab, var(--text), transparent 85%);
  background: var(--panel);
  color: var(--text);
  cursor: pointer;
  user-select: none;
  transition: .2s ease;
}

.chip.active{
  background: linear-gradient(135deg, var(--accent), var(--accent-2));
  color: #fff;
  border-color: transparent;
}

/* Muted helper text */
.muted{
  color: color-mix(in oklab, var(--text), transparent 40%);
  font-size: 18px;
  text-shadow: 0 1px 6px rgba(0,0,0,.45);
}

/* Grid & cards */
.grid{
  display: grid;
  gap: 16px;
  grid-template-columns: repeat(12, 1fr);
}

.card{
  grid-column: span 12;
  display: grid;
  grid-template-columns: 1.1fr 1.6fr;
  background: var(--panel);
  border: 1px solid color-mix(in oklab, var(--text), transparent 90%);
  border-radius: var(--radius);
  overflow: hidden;
  box-shadow: var(--shadow);
  min-height: 160px;
}

/* Left image column */
.thumb{
  position: relative;
  min-height: 160px;
  background: var(--panel);
  overflow: hidden;
}
.thumb img{
  width: 100%;
  height: 100%;
  object-fit: cover;
  display: block;
  transition: transform .4s ease, filter .4s ease;
}
.card:hover .thumb img{
  transform: scale(1.05);
  filter: brightness(1.05);
}

/* Subtle idle motion on touch devices */
@media (hover: none){
  .thumb img{ animation: slowZoomCard 10s ease-in-out infinite alternate; }
}
@keyframes slowZoomCard { from { transform: scale(1) } to { transform: scale(1.04) } }

/* Desktop: gentle idle motion; pause on hover */
@media (hover: hover){
  .thumb img{ animation: slowZoomCard 16s ease-in-out infinite alternate; }
  .card:hover .thumb img{ animation-play-state: paused; }
}

/* Reduced motion */
@media (prefers-reduced-motion: reduce){
  .thumb img{ animation: none !important; transition: none !important; }
}

/* Fallback thumbnail background */
.thumb.fallback{
  background:
    linear-gradient(135deg, color-mix(in oklab, var(--accent-2), transparent 70%), transparent),
    linear-gradient(180deg, color-mix(in oklab, var(--accent), transparent 70%), transparent);
}

/* Content area */
.content{ padding: 16px; }

.card h3{
  margin: 4px 0 8px;
  font-size: 20px;
}

.badge{
  display: inline-flex;
  gap: 6px;
  align-items: center;
  background: rgba(255,255,255,.15);
  color: #fff;
  padding: 6px 10px;
  border-radius: 999px;
  font-size: 12px;
  font-weight: 700;
  text-shadow: 0 1px 6px rgba(0,0,0,.35);
}

.actions{
  display: flex;
  gap: 10px;
  margin-top: 10px;
  flex-wrap: wrap;
}

/* Buttons used for Code / More details */
.btn,
.btn-secondary{
  padding: 10px 14px;
  border-radius: 12px;
  font-weight: 600;
  cursor: pointer;
  display: inline-flex;
  align-items: center;
  gap: 8px;
  text-decoration: none;
  transition: transform .2s ease, box-shadow .2s ease, background .2s ease, border-color .2s ease, color .2s ease;
}

/* Primary button (e.g., More details) */
.btn{
  background: linear-gradient(135deg, var(--accent), var(--accent-2));
  color: #fff;
  border: none;
  box-shadow: 0 6px 16px rgba(0,0,0,.25);
}
.btn:hover{
  transform: translateY(-1px);
  box-shadow: 0 10px 24px rgba(0,0,0,.28);
}

/* Glowing effect used by cards */
.glow-card,
.qcard { position: relative; overflow: hidden; }
.qcard .glowing-effect { z-index: 0; }
.qcard > * { position: relative; z-index: 1; }
.glow-card { isolation: isolate; }
.glow-card .glowing-effect { z-index: 0; }
.glow-card > * { position: relative; z-index: 1; }

body.home-has-bg::before{
  content:"";
  position: fixed;
  inset: 0;
  background: rgba(0,0,0,.28);
  pointer-events: none;
  z-index: 0;
}
header, main, footer, .hero { position: relative; z-index: 1; }

.glowing-effect{
  position: absolute;
  inset: 0;
  border-radius: inherit;
  pointer-events: none;
  z-index: 0;
  filter: blur(var(--ge-blur, 0px));
}
.glowing-effect::after{
  content: "";
  position: absolute;
  inset: calc(-1 * var(--ge-border, 1px));
  border-radius: inherit;
  border: var(--ge-border, 1px) solid transparent;
  background:
    radial-gradient(circle at var(--ge-gx,50%) var(--ge-gy,50%), rgba(221,123,187,0.65) 0%, rgba(221,123,187,0) 30%),
    radial-gradient(circle at calc(var(--ge-gx,50%) - 10%) calc(var(--ge-gy,50%) - 10%), rgba(215,159,30,0.45) 0%, rgba(215,159,30,0) 25%),
    repeating-conic-gradient(from 236.84deg at 50% 50%,
      #dd7bbb 0%,
      #d79f1e calc(25% / var(--ge-times, 5)),
      #5a922c calc(50% / var(--ge-times, 5)),
      #4c7894 calc(75% / var(--ge-times, 5)),
      #dd7bbb calc(100% / var(--ge-times, 5))
    );
  background-attachment: local;
  background-position: center;
  opacity: var(--ge-active, 0);
  transition: opacity .18s ease, transform .3s ease;
  -webkit-mask-clip: padding-box, border-box;
  mask-clip: padding-box, border-box;
  -webkit-mask-composite: source-in;
  mask-composite: intersect;
  -webkit-mask-image: linear-gradient(#0000, #0000),
    conic-gradient(
      from calc((var(--ge-start, 0) - var(--ge-spread, 40)) * 1deg),
      transparent 0deg,
      #fff 1deg,
      transparent calc(var(--ge-spread, 40) * 2deg)
    );
  mask-image: linear-gradient(#0000, #0000),
    conic-gradient(
      from calc((var(--ge-start, 0) - var(--ge-spread, 40)) * 1deg),
      transparent 0deg,
      #fff 1deg,
      transparent calc(var(--ge-spread, 40) * 2deg)
    );
}
.qcard > *{ position: relative; z-index: 1; }
.btn-secondary:hover{
  border-color: color-mix(in oklab, var(--text), transparent 65%);
  background: color-mix(in oklab, var(--text), transparent 90%);
}

/* Empty state */
.empty{
  text-align: center;
  color: var(--muted);
  padding: 32px;
  border: 1px dashed color-mix(in oklab, var(--text), transparent 85%);
  border-radius: 12px;
}

/* Footer */
footer{
  padding: 36px 0;
  color: var(--muted);
  text-align: center;
}

/* Modal */
.modal{
  position: fixed;
  inset: 0;
  background: rgba(0,0,0,.7);
  display: flex;
  align-items: center;
  justify-content: center;
  z-index: 1000;
}
.modal[hidden]{ display: none; }
.modal-content{
  background: var(--panel);
  color: var(--text);
  padding: 24px;
  border-radius: 12px;
  max-width: 700px;
  max-height: 80vh;
  overflow: auto;
  box-shadow: var(--shadow);
}
.close{
  float: right;
  font-size: 24px;
  cursor: pointer;
  background: transparent;
  border: none;
  color: var(--text);
}

/* ===== Distinct chips for Language vs Type ===== */
.chip--lang.active {
  background: linear-gradient(135deg, var(--accent), var(--accent-2));
  color: #fff;
  border-color: transparent;
}
/* Type chips */
.chip--type.active {
   background: linear-gradient(135deg, #10b981, #34d399);
  color: #fff;
  border-color: transparent;
}
.chip--type.is-hackathon.active {
   background: linear-gradient(135deg, #10b981, #34d399);
}

/* Badges by type */
.badge--type {
  background: linear-gradient(135deg, #a78bfa33, #06b6d433);
  color: #fff;
}
.badge--type.badge--academic,
.badge--type.badge--personal,
.badge--type.badge--hackathon {
  background: linear-gradient(135deg, #10b981, #34d399);
  color: #fff;
}

/* =========================================================
   Athletics
   ========================================================= */
.hero-wrap { position: relative; height: 90vh; min-height: 560px; overflow: hidden; }
.hero-slides { position: absolute; inset: 0; }
.hero-slide {
  position: absolute; inset: 0; background: center/cover no-repeat;
  opacity: 0; transition: opacity 1s ease; filter: brightness(.9); will-change: transform, opacity;
}
.hero-slide.active { opacity: 1; animation: zoomSlow 8s ease-in-out forwards; }
.hero-overlay {
  position: absolute; inset: 0;
  background: linear-gradient(180deg, rgba(0,0,0,.4), rgba(0,0,0,.3) 40%, rgba(0,0,0,.5));
  pointer-events: none;
}
.hero-inner {
  position: relative; z-index: 2; height: 100%;
  display: flex; align-items: flex-end; justify-content: center;
  padding: 28px 18px; padding-bottom: 8vh; text-align: left;
}
.hero-inner .about { max-width: 780px; }
.hero-inner .about p { margin: 0; text-indent: 0; line-height: 1.45; }

.team-section { position: relative; height: 90vh; min-height: 560px; overflow: hidden; margin-top: 22px; }
.team-bg { position: absolute; inset: 0; }
.team-bg .frame {
  position: absolute; inset: 0; background: center/cover no-repeat;
  opacity: 0; transition: opacity 1s ease; filter: brightness(.88); will-change: transform, opacity;
}
.team-bg .frame.active { opacity: 1; animation: zoomSlow 8s ease-in-out forwards; }
.team-overlay { position: absolute; inset: 0; background: linear-gradient(180deg, rgba(0,0,0,.55), rgba(0,0,0,.45)); }
.team-inner {
  position: relative; z-index: 2; height: 100%;
  display: flex; align-items: flex-end; justify-content: center;
  padding: 28px 18px; padding-bottom: 8vh; text-align: left;
}
.team-inner .about { max-width: 780px; }
.team-inner .about p { margin: 0; text-indent: 0; line-height: 1.45; }

.dots {
  position: absolute; left: 50%; transform: translateX(-50%);
  bottom: 16px; display: flex; gap: 8px; z-index: 3;
}
.dot { width: 9px; height: 9px; border-radius: 50%; background: rgba(255,255,255,.6); border: none; cursor: pointer; }
.dot.active { background: #fff; }

@keyframes zoomSlow { from { transform: scale(1.03) } to { transform: scale(1.07) } }
@media (hover: none) {
  .hero-slide.active, .team-bg .frame.active { animation: zoomSlowAlt 14s ease-in-out infinite alternate; }
  @keyframes zoomSlowAlt { from { transform: scale(1.02) } to { transform: scale(1.06) } }
}
@media (prefers-reduced-motion: reduce) {
  .hero-slide, .team-bg .frame { animation: none !important; transition: none !important; }
}

.records { position: relative; }
.records-bg {
  position: absolute; inset: 0; background: url('/photos/PB.jpg') center/cover no-repeat;
  filter: brightness(.4); z-index: 0;
}
.records-inner { position: relative; z-index: 1; color: #fff; text-align: center; padding: 80px 20px; }
.records-inner h2 { font-size: 36px; margin-bottom: 20px; }
.records-inner ul { list-style: none; padding: 0; margin: 0; font-size: 20px; line-height: 1.8; }

.hero-inner .about h1,
.hero-inner .about p,
.team-inner .about h2,
.team-inner .about p { color: #fff; text-shadow: 0 2px 8px rgba(0,0,0,.35); }

@media (max-width:640px){
  .hero-inner .about h1{ font-size:34px !important; line-height:1.15; }
  .team-inner .about h2{ font-size:28px !important; line-height:1.2; }
}
.hero-inner .about h1 { -webkit-user-select: none; user-select: none; background: transparent !important; }
.hero-inner .about h1::selection { background: transparent; color: inherit; }
.hero-inner .about h1::-moz-selection { background: transparent; color: inherit; }

/* GlowingEffect (required styles) - duplicate-safe */
.qcard { position: relative; overflow: hidden; }
.glowing-effect{ position: absolute; inset: 0; border-radius: inherit; pointer-events: none; filter: blur(var(--ge-blur, 0px)); z-index: 0; }
.glowing-effect::after{
  content: ""; position: absolute; inset: calc(-1 * var(--ge-border, 1px)); border-radius: inherit;
  border: var(--ge-border, 1px) solid transparent;
  background:
    radial-gradient(circle, #dd7bbb 10%, #dd7bbb00 20%),
    radial-gradient(circle at 40% 40%, #d79f1e 5%, #d79f1e00 15%),
    radial-gradient(circle at 60% 60%, #5a922c 10%, #5a922c00 20%),
    radial-gradient(circle at 40% 60%, #4c7894 10%, #4c789400 20%),
    repeating-conic-gradient(
      from 236.84deg at 50% 50%,
      #dd7bbb 0%,
      #d79f1e calc(25% / var(--ge-times, 5)),
      #5a922c calc(50% / var(--ge-times, 5)),
      #4c7894 calc(75% / var(--ge-times, 5)),
      #dd7bbb calc(100% / var(--ge-times, 5))
    );
  background-attachment: local; background-position: center; opacity: var(--ge-active, 0); transition: opacity .18s ease, transform .3s ease;
  -webkit-mask-clip: padding-box, border-box; mask-clip: padding-box, border-box;
  -webkit-mask-composite: source-in; mask-composite: intersect;
  -webkit-mask-image: linear-gradient(#0000, #0000),
    conic-gradient(from calc((var(--ge-start, 0) - var(--ge-spread, 20)) * 1deg), transparent 0deg, #fff, transparent calc(var(--ge-spread, 20) * 2deg));
  mask-image: linear-gradient(#0000, #0000),
    conic-gradient(from calc((var(--ge-start, 0) - var(--ge-spread, 20)) * 1deg), transparent 0deg, #fff, transparent calc(var(--ge-spread, 20) * 2deg));
}

/* =========================================================
   Responsive
   ========================================================= */
@media (max-width: 980px){
  .hero .inner{ flex-direction: column; text-align: center; align-items: center; }
  .qcard{ grid-column: span 12; }
  .card{ grid-template-columns: 1fr; }
}
@media (max-width: 640px){
  .skill-card{ min-width: 280px; }
  .headline p{ font-size: 18px; }
  .headline-title{ flex-direction: column; align-items: center; gap: 8px; }
  .headline-title img{ width: 86px; height: 86px; }
  .headline .tagline{ display: inline-block; white-space: nowrap; font-size: clamp(12px, 3.6vw, 18px); }
  .about h2, .about p{ text-align: left; padding-left: 8px; }
  .card{ min-height: auto; }
}

/* Topbar and contact inline styles */
.topbar{ display:flex; justify-content:space-between; align-items:center; gap:12px; padding:6px 0; }
.topbar .bar{ display:flex; gap:12px; align-items:center; }
.nav-contacts{ display:flex; gap:12px; align-items:center; font-size:14px; }
.nav-contacts a{ color:#fff; text-decoration:none; font-weight:600; }
.nav-contacts a:hover{ opacity:0.9; text-decoration:underline; }
.contact-inline{ display:flex; gap:10px; align-items:center; }
.contact-inline .sep{ opacity:0.6; padding:0 6px; }

/* Background controls (Home) */
.bg-btn{
  position: absolute; top: 20px; width: 40px; height: 40px; border-radius: 999px; border: none;
  display: inline-flex; align-items: center; justify-content: center;
  background: rgba(0,0,0,0.45); color: #fff; cursor: pointer;
  box-shadow: 0 6px 18px rgba(0,0,0,0.45);
  transition: transform .18s ease, background .15s ease, opacity .15s ease;
}
.bg-btn.left{ left: 20px; }
.bg-btn.right{ right: 20px; }
.bg-btn:hover{ transform: translateY(-2px); background: rgba(255,255,255,0.06); }
.bg-btn:active{ transform: translateY(0); }

/* Skills specific links */
#skills { color: inherit; }
#skills a, #skills .skill-card a { color: #fff; font-weight: 600; text-decoration: none; }
#skills a:hover, #skills .skill-card a:hover { opacity: 0.9; text-decoration: underline; }
#skills .contact-inline{ display:flex; gap:10px; align-items:center; }

/* Skills scroll */
.scroll-wrap{ position: relative; display:flex; align-items:center; }
.skill-track{ padding: 8px 48px; gap: 18px; scroll-behavior: smooth; }
.scroll-btn{
  position: absolute; top: 50%; transform: translateY(-50%);
  width: 44px; height: 44px; border-radius: 999px; border: none;
  background: rgba(0,0,0,0.45); color: #fff; display: inline-flex; align-items: center; justify-content: center;
  cursor: pointer; box-shadow: 0 10px 24px rgba(2,6,23,0.6);
  transition: transform .14s ease, opacity .12s ease, background .12s ease; z-index: 5;
}
.scroll-btn.left{ left: 8px; }
.scroll-btn.right{ right: 8px; }
.scroll-btn:hover{ transform: translateY(-50%) scale(1.04); background: rgba(255,255,255,0.06); }
.scroll-wrap::before, .scroll-wrap::after{ display: none; }

.resume-actions{ display:flex; justify-content:flex-start; }
.resume-btn{
  display:inline-flex; align-items:center; gap:10px; padding: 12px 16px; border-radius: 12px;
  background: linear-gradient(135deg, rgba(135, 94, 255, 0.15), rgba(6,182,212,0.12));
  border: 1px solid rgba(255,255,255,0.06); color: #fff; font-weight:700;
  box-shadow: 0 8px 26px rgba(2,6,23,0.6); text-decoration:none;
  transition: transform .14s ease, box-shadow .14s ease, background .12s ease;
}
.resume-btn::before{ content: '\1F4C4'; font-size:18px; }
.resume-btn:hover{ transform: translateY(-3px); box-shadow: 0 14px 36px rgba(2,6,23,0.7); }

/* =========================================================
   Resume page
   ========================================================= */
.resume-page {
  min-height: 100vh;
  display: flex;
  flex-direction: column;
    backdrop-filter: blur(8px);
  -webkit-backdrop-filter: blur(8px);
  background: linear-gradient(135deg, #0a2540, #102c54, #1b3b73, #0d47a1);
  border-bottom: 1px solid rgba(255, 255, 255, 0.15);
  color: #f0f4ff;
  box-shadow: 0 2px 8px rgba(0, 0, 0, 0.35);
  background-size: 400% 400%;
  animation: resumeGradientFlow 18s ease infinite;
  color: var(--text);
}

/* ---- Header overrides only inside Resume page ---- */
.resume-page header{
  backdrop-filter: blur(8px);
  -webkit-backdrop-filter: blur(8px);
  background: linear-gradient(135deg, #0a2540, #102c54, #1b3b73, #0d47a1);
  border-bottom: 1px solid rgba(255, 255, 255, 0.15);
  color: #f0f4ff;
  box-shadow: 0 2px 8px rgba(0, 0, 0, 0.35);
}
.resume-page header .nav{ padding: 10px 0; }
.resume-page header .nav a{
  color: #f0f4ff; opacity: 1; text-shadow: none; font-weight: 600;
}
.resume-page header .nav a:hover{ text-decoration: underline; }
.resume-page header .sep{ opacity: .65; margin: 0 6px; color: #c5d4f7; }

/* ---- Resume typography spacing ---- */
.resume-section h2 {
  margin-top: 28px;
  margin-bottom: 10px;
  font-size: 22px;
  font-weight: 700;
  color: #fff;
}
.resume-section h3 {
  margin-top: 20px;
  margin-bottom: 6px;
  font-size: 18px;
  font-weight: 600;
  color: #e0e6f5;
}
.resume-section p,
.resume-section ul {
  margin-top: 0;
  margin-bottom: 6px;
  line-height: 1.6;
}

.see-more, .see-less {
  color: #4da6ff;
  cursor: pointer;
  font-weight: 600;
  display: inline-block;
  margin-top: 6px;
}
.see-more:hover, .see-less:hover { text-decoration: underline; }

.courses ul.sublist {
  margin: 6px 0 0 20px;
  font-size: 15px;
  line-height: 1.5;
}

@keyframes resumeGradientFlow {
  0% { background-position: 0% 50%; }
  50% { background-position: 100% 50%; }
  100% { background-position: 0% 50%; }
}

.resume-paper {
  background: rgba(255, 255, 255, 0.07);
  backdrop-filter: blur(10px);
  border-radius: var(--radius);
  padding: 2rem;
  box-shadow: var(--shadow);
}

/* Small helpers */
@media (max-width:640px){
  .scroll-btn{ width:36px; height:36px; }
  .scroll-wrap::before, .scroll-wrap::after{ width: 28px; }
}
